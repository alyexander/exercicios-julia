# Crie um programa que leia dois números e mostre a soma entre eles.
print("Digite um número: ")
α = parse(Int64, readline())

print("Digite outro número: ")
β = parse(Int64, readline())

γ = α + β

print("$α + $β = $γ")